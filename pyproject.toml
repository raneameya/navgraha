[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["core*"]

[project]
name = "chart_now"
version = "0.1.0"
dependencies = [
    "shiny",
    "pandas",
    "matplotlib",
    "tzdata"
]
